[ui]
# name and email
username = Becky Smith <becky.smith@ecometrica.com>

# set editor to vim and resolve merge conflicts to mimic git method
editor = vim
merge = internal:merge

[experimental]
crecord=True

[extensions]
# uncomment these lines to enable some popular extensions
# (see "hg help extensions" for more info)
#
# pager =
progress =
color =
shelve =
strip =
purge =