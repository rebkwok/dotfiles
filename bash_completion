# Git autocomplete:
[ -f $(brew --prefix)/git/contrib/completion/git-completion.bash ] \
    && . $(brew --prefix)/git/contrib/completion/git-completion.bash

# Add aws completion
complete -C aws_completer aws

echo 'Bash completion completed (.bash_completion)'
